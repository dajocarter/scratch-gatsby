import React, { Component } from "react";

class StaggeredRowsLayout extends Component {
  render() {
    const layout = this.props.layout;
    return (
      <div>
        <h1>Staggered Rows</h1>
      </div>
    );
  }
}

export default StaggeredRowsLayout;
